[pytest]
# Without this, `pytest` with no arguments also scans output/ (generated
# reports, geocode cache, old test-run scratch folders) and can fail on
# stale locked directories left over from a previous interrupted run —
# found 2026-09-05 while auditing the app. Tests only ever live in tests/.
testpaths = tests
